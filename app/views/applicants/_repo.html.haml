.m-experience{ class: type }
  .display
    %a.name{ href: repo.url, target: "new_tab" }
      = fa_icon "github"
      = "#{repo.name}"
    %p.time= "#{repo.pretty_date}"
    .languages
      - repo.languages.first(3).each do |lang|
        = "#{lang.skill.name} - #{lang.percent}%, "
