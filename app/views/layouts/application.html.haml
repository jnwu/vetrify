!!!
%html.wf-loading
  %head
    %title= content_for?(:title) ? yield(:title) + " - Vetrify" : "Vetrify - New Graduate Jobs"
    =stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    =javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
    %meta{ name: "description", content: "Vetrify matches students to jobs and allows them to apply with a simple process minimizing time spent on applications and maximizing application impact for students." }

  %body
    = render 'shared/header'
    = yield
